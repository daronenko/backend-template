service:
  name: backend-template

  adminKey:

  logger:
    level: debug
    devMode: false
    logsPath: /var/log/dump.log
    maxSize: 10
    maxBackups: 5
    maxAge: 30
    compress: true

postgres:
  host:
  port:
  user:
  password:
  database:
  sslMode:
